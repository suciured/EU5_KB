mvi_transfer_vassal = {

	type = subject
	category = CATEGORY_SUBJECT_ACTIONS

	ai_tick = never

	potential = {
	}

	allow = {
	}

	select_trigger = {
		looking_for_a = country
		interaction_source_list = {
			scope:actor = {
				every_subject = {
					add_to_list = source
				}
			}
		}
		target_flag = recipient
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
		}
        enabled = {
		}
	}

	select_trigger = {
		looking_for_a = country
		interaction_source_list = {
			scope:actor = {
				every_subject = {
					limit = {
						NOT = { this = scope:recipient }
					}
					add_to_list = source
				}
			}
		}
		target_flag = target_country
		name = "Choose the vassal to transfer"
		column = {
			data = name
		}
		visible = {
		}
        enabled = {
		}
	}

	select_trigger = {
		looking_for_a = subject_type
		target_flag = target_subject_type
		name = "Choose the subject type"
		column = {
			data = subject_type_icon
		}
		column = {
			data = name
		}
		column = {
			data = subject_type_attribute_column
		}
		default_sort = name
		visible = {
		}
        enabled = {
		}
	}

	effect = {
		scope:target_country = {
			make_subject_of = {
				target = scope:recipient
				type = scope:target_subject_type
			}
		}
	}
}
